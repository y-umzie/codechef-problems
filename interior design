#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int t,a,b,c,d;
	cin>>t;
	for(int i=0;i<t;i++)
	{
	    cin>>a>>b>>c>>d;
	    int p=a+b;
	    int q=c+d;
	    if(p<q)
	    {
	        cout<<p<<endl;
	    }
	    else
	    {
	        cout<<q<<endl;
	    }
	}
	return 0;
}
